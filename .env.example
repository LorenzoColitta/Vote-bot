# Discord bot token and application client ID
DISCORD_TOKEN=your_discord_bot_token
CLIENT_ID=your_application_client_id

# Database file path (optional)
DB_PATH=./data/vote-bot.db

# Secret used to HMAC voter ids so votes are anonymous in DB
# KEEP THIS SECRET PRIVATE. If someone has DB and VOTE_SECRET they can correlate.
VOTE_SECRET=replace_with_a_long_random_string